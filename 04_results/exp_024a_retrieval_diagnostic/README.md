# exp024a：历史相似截面检索诊断

状态：`completed_rejected`。

本阶段只产生诊断指标和负对照，不训练新预测模型，不读取 Test 标签，不生成 `prediction.npy`，线上提交预算为 0。

基线说明：official Valid 使用已冻结的 exp021 全量预测；三个早期 OOF 窗口使用 exp016 严格 OOF 六家族的静态秩融合代理。两个口径分别报告，不混合成一个声称为 exp021 的分数。

## 结果

- 相似状态检索胜过随机对照：`2/4` 个窗口，未达到 `4/4` 门槛。
- 相似状态检索胜过最近窗口：`2/4` 个窗口，未达到至少 `3/4` 门槛。
- official Valid：检索校正相对 exp021 `+0.000637`，但随机历史对照为 `+0.001776`，不支持“状态相似性带来增益”的假设。
- 检索置信度与逐截面增益的 leave-one-block-out Spearman 中位数为 `-0.121613`，8 次留块相关全部为负。
- 三个受保护预测哈希保持不变；没有打开 Test 数组、生成预测或使用线上提交额度。

结论：按预注册规则停止 SRRRS 检索路线，不执行 exp024b，也不通过调整 K、PCA、距离或 alpha 追逐本次结果。
