# 01 数据分析

状态：`completed`（数据分析已完成，结果由根目录项目状态源统一登记）。

入口：`data_analysis.ipynb`

本阶段负责理解原始数据，不训练模型。主要内容包括数据结构、时间切分、股票池、掩码、标签分布、特征质量、漂移、单特征 RankIC 和类别特征分析。

Notebook 直接读取项目根目录的 `data.z`。第一次完整读取可能耗时较长，本目录不参与模型训练。
