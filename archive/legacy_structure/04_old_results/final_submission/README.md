# 最终提交

状态：`archived`（不代表当前正式提交）。

当前提交文件：`y1_final.npy`

提交前检查：

- 形状：`(442, 5282)`；
- 类型：`float32`；
- 全部为有限值；
- 测试评估位置：`2,042,538`；
- 非评估位置：严格等于 `0.5`；
- 使用 `np.load("y1_final.npy", allow_pickle=False)` 可读取。

`y1_final.metadata.json` 保存上述验收信息和来源路径。
