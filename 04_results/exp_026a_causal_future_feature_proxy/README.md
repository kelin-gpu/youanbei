# exp026a：合规未来特征预测代理诊断

状态：`completed_rejected`。

本实验不会读取真实未来 Test 特征。它仅在历史 Train 中学习 `X_j(t-10:t) -> X_j(t+s)`，并在 Valid 的每个 t 使用截至 t 的输入生成预测代理。Valid 截止到 3146，确保评估目标 `t+s` 仍在 Valid 内。

不生成 Test prediction，不使用线上提交额度。

## 结果

- 多个未来特征可以被历史部分预测，例如 `num_348@+5` 的未来特征 RankIC 为 `0.372240`；
- 但预测代理与 exp021 融合后的 Valid delta 为 `-0.000592`；
- 同口径 persistence 代理 delta 为 `-0.000824`，forecast 仅领先 `+0.000232`，低于预注册 `+0.0005`；
- forecast 的三个时间段 delta 为 `[-0.001142, +0.000019, -0.000653]`；
- 未读取 Test 数组、未生成预测、未使用线上额度，保护哈希不变。

结论：可预测的未来特征成分没有携带可迁移的 y(t) 增量；停止 exp026b，不增加非线性模型追逐本次 Valid。
