# Legacy Structure

状态：`archived`（历史目录，仅供追溯，不作为当前实现入口）。

本目录保存架构重构前的数据处理入口、训练入口和旧结果目录，仅用于历史核对。

日常运行请使用项目根目录下的 `01_analysis`、`02_experiments`、`03_cache` 和 `04_results`。不要从本目录启动新的实验，也不要把这里的旧输出复制回正式结果目录。
